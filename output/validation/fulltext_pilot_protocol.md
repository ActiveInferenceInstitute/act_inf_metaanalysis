# Full-text evidence pilot protocol

Schema version: `1.0`; deterministic seed: `42`.

Select only the queued open-full-text records. Record the exact source section and page/location for each hypothesis-relevant quote. This pilot does not modify the primary abstract-only nanopublications or scores. Record access failures and ambiguous passages rather than inferring them.
